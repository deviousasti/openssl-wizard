<Window x:Class="openssl_wizard.Progress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Processing" Height="150" Width="400" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" AllowsTransparency="True" WindowStyle="None"
        >
    <Border BorderThickness="2" BorderBrush="DarkGray">
        <StackPanel Margin="10">
            <TextBlock Margin="0,10" FontSize="16">OpenSSL</TextBlock>
            <ProgressBar IsIndeterminate="True" Height="20"  />
            <Separator />
            <StackPanel Style="{StaticResource Buttons}">
                <Button HorizontalAlignment="Center">Cancel</Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
